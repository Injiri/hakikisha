<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:elevation="6dp"
    android:paddingTop="12dp"
    android:background="@android:color/white"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:layout_marginStart="12dp"
        android:id="@+id/purchase_form_cancel_button"
        android:src="@drawable/ic_action_close_light"
        android:layout_alignParentStart="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/purchase_form_submit_button"
        android:layout_marginEnd="12dp"
        android:text="Save"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        android:background="@drawable/deep_blue_medium_curved_background"
        android:layout_alignParentEnd="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <EditText
        android:paddingBottom="18dp"
        android:id="@+id/purchase_product_name_editText"
        android:layout_marginTop="38dp"
        android:hint="Product/Service Descrption"
        android:textColor="@color/dark_grey"
        android:textSize="24sp"
        android:background="@null"
        android:layout_marginStart="36dp"
        android:layout_below="@+id/order_cancel_form_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <ProgressBar
        android:id="@+id/register_purchase_progress_bar"
        android:elevation="4dp"
        android:visibility="gone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</RelativeLayout>
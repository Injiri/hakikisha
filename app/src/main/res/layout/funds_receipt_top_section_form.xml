<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:elevation="6dp"
    android:paddingTop="12dp"
    android:id="@+id/payment_receipt_cancel_button"
    android:background="@android:color/white"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:layout_marginStart="12dp"
        android:id="@+id/order_cancel_form_button"
        android:src="@drawable/ic_action_close_light"
        android:layout_alignParentStart="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/payment_receved_submit_button"
        android:layout_marginEnd="12dp"
        android:text="Save"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        android:background="@drawable/deep_blue_medium_curved_background"
        android:layout_alignParentEnd="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:layout_marginStart="22dp"
        android:id="@+id/payment_receipt_title"
        android:layout_below="@+id/payment_receved_submit_button"
        android:textColor="@android:color/black"
        android:textSize="24sp"
        android:text="Payment Receipt Form"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <ProgressBar
        android:id="@+id/payment_receipt_progress_bar"
        android:layout_below="@+id/payment_receipt_title"
        android:visibility="gone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <EditText
        android:paddingBottom="18dp"
        android:id="@+id/payment_receipt_amount_received_edit_text"
        android:inputType="number"
        android:layout_marginTop="18dp"
        android:hint="Amount Received... ##.# Ksh"
        android:textColor="@color/dark_grey"
        android:textSize="24sp"
        android:background="@null"
        android:layout_marginStart="36dp"
        android:layout_below="@+id/payment_receipt_progress_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</RelativeLayout>